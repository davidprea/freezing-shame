%fieldset
	%legend House Rules
	-rules=HouseRule.rules
	-rules.keys.each do | key |
		%p
			%input{ :type => "checkbox", :title => rules[key][:tooltip], :name => key }
				%label{ :title => rules[key][:tooltip] }
					=rules[key][:name]
	